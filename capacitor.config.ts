import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qizuna_builder_frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
